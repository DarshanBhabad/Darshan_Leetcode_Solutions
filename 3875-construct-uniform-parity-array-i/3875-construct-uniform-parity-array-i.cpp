class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        int n=nums1.size();
        // If the array contains at most one odd number, the answer is true. Otherwise, it's false
        

        
//
// If all elements are odd → already uniform → true
// If all elements are even → already uniform → true
// If there are both odd and even numbers, the answer depends on whether the required operation can make them all equal.

return true; 


//ans will be always true 
// if odd and veen both present use odd number as a subtractor and keep the oddd number
//odd number will make all evbens odd and we keep all odds same
        

    }
};